/**
 * GoogleNavOverlay.jsx — src/Components/GoogleNavOverlay.jsx
 *
 * Returned null as per user requirement to remove all green turn cards & speed pills.
 */

export default function GoogleNavOverlay() {
  return null;
}
